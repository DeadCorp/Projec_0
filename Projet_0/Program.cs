using System;
using System.Linq;
using System.Threading;

namespace Projet_0
{
    
    internal  class Program
    {
        private static void Main(){
            var tuple = (years_passed: 10, term: 25);
            Console.WriteLine("Allo");
            Console.WriteLine("It's add in new branch");
            int[] h = { 10, 2, 3, 4, 5 };
            foreach (var num in h)
            {
                Console.WriteLine("Value" + num);
            }



            Console.ReadKey();
        }
    }
}